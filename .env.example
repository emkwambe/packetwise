# PacketWise Configuration

# App
DEBUG=true
DATABASE_URL=sqlite:///data/packetwise.db

# OCR
TESSERACT_CMD=tesseract

# SMTP (optional — for exception email alerts)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
UNDERWRITING_EMAIL=underwriting@demo.bank
